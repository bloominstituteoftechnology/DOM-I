const counter = 0;
const digits = [];
// const time = window.setInterval();
// if counter = 10000{ color red};
// 10ms = digit++
//
// array = all digits;
//
// loop through array change every digit 10ms;
digits = document.querySelectorAll('.digits')
digits = digits.childNodes;
let test = 'hi';
window.setInterval(function(){ alert("Hello"); }, 3000);
window.alert(digits);
 for(;counter < 10000; counter++){

 }
