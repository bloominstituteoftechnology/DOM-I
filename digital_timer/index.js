// variables
let msTens = document.getElementById('msTens');
let msHuns = document.getElementById('msHundreds');
let secOnes = document.getElementById('secondOnes');
let secTens = docment.getElementById('secondTens');

// set times to 0
msTens.innerHTML = 0;
msHuns.innerHTML = 0;
secOnes.innerHTML = 0;
secTens.innerHTML = 0;

// timer function
function timer() {
    if (msTens.innerHTML < 9) {
        msTens.innerHTML++;
    } else {

        if (msHuns.innerHTML < 9)
    }
}


 